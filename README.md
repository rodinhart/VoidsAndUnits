# VoidsAndUnits

An example implementation of Noughts and Crosses (Tic Tac Toe) using PureScript, semantic-ui and react.

## install
`npm install`
`bower install`

## compile
`npm run compile`

## start
`npm start`

## browse
`localhost:3000/`
